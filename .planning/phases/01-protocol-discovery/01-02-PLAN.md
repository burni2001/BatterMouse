---
phase: 01-protocol-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - discover/discover.py
  - discover/FINDINGS.md
autonomous: false
requirements:
  - Prerequisite for HID-01
must_haves:
  truths:
    - "Running `python discover.py` prints a hex+decimal table of 32-byte input reports when the mouse is moved"
    - "At least one candidate byte in the 0–100 range is visible in the printed output"
    - "FINDINGS.md has Usage Page, report ID (or 'none'), and Battery Byte Offset filled in with real values (not TBD)"
    - "A second reading at a different battery state confirms the identified byte changes value"
  artifacts:
    - path: "discover/discover.py"
      provides: "Complete discovery script — enumerate + feature scan + input report stream; all three strategies runnable"
      min_lines: 100
    - path: "discover/FINDINGS.md"
      provides: "Documented protocol: Usage Page, report ID, byte offset, value range, confirmation method"
      contains: "Battery Byte Offset"
  key_links:
    - from: "discover/discover.py"
      to: "hid input report stream"
      via: "read_input_reports() printing 32-byte rows"
      pattern: "read\\(32"
    - from: "discover/FINDINGS.md"
      to: ".planning/phases/01-protocol-discovery/01-02-SUMMARY.md"
      via: "SUMMARY references FINDINGS for Phase 2 handoff"
      pattern: "Battery Byte Offset"
---

<objective>
Run the full discovery session against the physical Keychron M3 dongle, identify the battery byte offset, and document all findings in FINDINGS.md so Phase 2 can build the C# app against confirmed values.

Purpose: This is the core deliverable of Phase 1 — converting the unknown byte offset from a blocker into a documented fact. Phase 2 cannot start without this.

Output: A completed `discover/FINDINGS.md` with Usage Page, report ID, byte offset, value range, and confirmation method. The `discover.py` script may be updated with a confirmed `--read-battery` mode that prints just the percentage.
</objective>

<execution_context>
@C:/Users/Bernhard/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bernhard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-protocol-discovery/01-RESEARCH.md
@.planning/phases/01-protocol-discovery/01-VALIDATION.md
@.planning/phases/01-protocol-discovery/01-01-SUMMARY.md

<interfaces>
<!-- From discover/discover.py (created in Plan 01) -->
<!-- Three functions available: -->

enumerate_tlcs() -> list[dict]
  # Returns all TLCs from hid.enumerate(VID, PID_DONGLE)
  # Each dict has: path (bytes), usage_page (int), usage (int), interface_number (int)
  # Prints formatted list; vendor TLCs marked "<-- VENDOR (candidate)"

scan_feature_reports(path: bytes) -> None
  # Opens device at path, tries GET_FEATURE_REPORT for IDs 0x00–0x1F
  # Prints non-zero responses as hex; prints "No non-zero feature reports found" if all zero

read_input_reports(path: bytes, duration_s: int = 60) -> None
  # Opens device at path (set_nonblocking=1), reads 32-byte reports for duration_s seconds
  # Prints hex+dec rows; move mouse to trigger reports
  # Ctrl+C stops early

# Main block (no args): enumerate -> scan_feature_reports -> read_input_reports for each vendor TLC
# Main block (--enumerate): enumerate only, exit 0/1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full discovery and capture output</name>
  <files>discover/session-1.log</files>
  <action>
Run the full discovery script against the physical device and capture output for analysis:

```bash
cd "D:/OneDrive/Coding Projects/BatterMouse"
python discover/discover.py 2>&1 | tee discover/session-1.log
```

Move the mouse actively during the 60-second input report window to generate reports.

After the run completes, analyze `discover/session-1.log`:

1. Check the enumeration section: note all vendor TLC usage pages and paths
2. Check the feature report scan: note any non-zero responses and which report IDs returned them
3. Check the input report rows: scan all 32 byte columns looking for values in the 0–100 range that appear stable (same value across multiple consecutive rows)

If the 01-01-SUMMARY.md already contains a confirmed byte offset from keychron-m3-linux main.py, verify that byte position in the log output against known battery state.

If the offset is still TBD, identify the candidate byte using this heuristic:
- The battery byte will be in the 0–100 range
- It will be stable (not changing every report like mouse X/Y coordinates do)
- It will NOT be 0x00 or 0xFF (those are padding/markers)
- It should match your physical knowledge of current battery level (fully charged = ~100, partially charged = ~50–80)

If mouse movement does not produce any reports (empty reads throughout), check:
- Is the mouse powered on?
- Is it connected via 2.4GHz dongle (not USB cable)?
- Is the correct TLC being opened (usage_page >= 0xFF00)?

If needed, add a brief sleep and retry loop to discover.py to tolerate slow-to-respond dongles, but do not fundamentally change the discovery approach.
  </action>
  <verify>
    <automated>test -f "D:/OneDrive/Coding Projects/BatterMouse/discover/session-1.log" && grep -q "hex\[" "D:/OneDrive/Coding Projects/BatterMouse/discover/session-1.log"</automated>
  </verify>
  <done>
- `discover/session-1.log` exists and contains at least one `hex[NNNN]` report row
- A candidate battery byte offset has been identified (or feature report ID confirmed)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Identify battery byte from session log</name>
  <files>discover/session-1.log</files>
  <action>Human reviews the session log to identify the battery byte offset. No code changes — this is a human analysis task.</action>
  <verify>
    <automated>MISSING — human provides byte offset via resume-signal before Task 2 runs</automated>
  </verify>
  <done>User has identified the battery byte offset (or a set of candidates) and provided it via resume-signal.</done>
  <what-built>Full discovery run against the physical Keychron M3 dongle. session-1.log contains the raw 32-byte input report dump.</what-built>
  <how-to-verify>
1. Open `discover/session-1.log` in a text editor or terminal
2. Find the `hex[NNNN]` / `dec[NNNN]` report rows
3. Look across each byte column — the battery byte will be:
   - A value in the 0–100 range
   - Stable across multiple consecutive readings
   - NOT varying wildly like the mouse X/Y position bytes
4. Check the decimal row for a value that matches your rough knowledge of current battery level
   - If the mouse is freshly charged: look for a byte showing ~100
   - If partially charged: look for a byte in the 40–80 range
5. Note the 0-indexed byte position of that value (columns are labeled 00–31)
6. If a feature report scan produced non-zero output, check those bytes too

If you can identify the battery byte with confidence, approve. If ambiguous, note which byte positions are candidates.
  </how-to-verify>
  <resume-signal>Type "approved: byte N" where N is the byte offset (e.g., "approved: byte 3"), or "ambiguous: bytes N,M are candidates" to proceed with multiple candidates</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Confirm battery byte and populate FINDINGS.md</name>
  <files>discover/discover.py, discover/FINDINGS.md</files>
  <action>
Using the byte offset confirmed (or narrowed to candidates) in the checkpoint, complete two steps:

**Step A — Add --read-battery mode to discover.py:**

Add a `--read-battery` flag that:
1. Enumerates TLCs, opens the first vendor TLC via `open_path()`
2. Reads input reports until one arrives (timeout up to 10 seconds, move mouse if needed)
3. Extracts byte at the confirmed offset and prints: `Battery: {value}%`
4. Exits 0

If multiple candidates exist from the checkpoint, print all candidates labeled by offset:
`Candidate byte {N}: {value}`

```bash
# Test immediately after writing:
python discover/discover.py --read-battery
```

Move the mouse if nothing prints within 5 seconds.

**Step B — Populate FINDINGS.md:**

Fill in the FINDINGS.md template with real values from the discovery session:
- Usage Page: (from enumeration output, e.g., 0xFF00)
- Usage ID: (from enumeration output)
- Interface Number: (from enumeration output)
- Report Type: Input Report (check both boxes if feature report also showed data)
- Report ID: (byte 0 of the input report rows — typically 0x01 or 0x00)
- Report Size: 32 bytes
- Battery Byte Offset: (the confirmed N from the checkpoint)
- Value at current charge: (value seen in session-1.log)
- Confirmation method: "Value sanity check against known charge state" (or describe what you actually did)

Add the C# translation section at the bottom:
```csharp
const int BatteryByteOffset = N;  // confirmed from keychron-m3-linux main.py + empirical session
int batteryPercent = report[BatteryByteOffset];
```

If the byte offset is still ambiguous (multiple candidates), document ALL candidates with notes on what makes each plausible. Phase 2 can start with the highest-confidence candidate and confirm during integration.
  </action>
  <verify>
    <automated>cd "D:/OneDrive/Coding Projects/BatterMouse" && python discover/discover.py --read-battery</automated>
  </verify>
  <done>
- `python discover/discover.py --read-battery` prints "Battery: N%" (or candidate list) without crashing
- FINDINGS.md has all fields populated with real values from the discovery session (no "TBD" remaining, or any remaining TBD explicitly explained)
- The C# translation block is present in FINDINGS.md
  </done>
</task>

</tasks>

<verification>
Final phase verification (run before creating SUMMARY):

1. Automated:
```bash
cd "D:/OneDrive/Coding Projects/BatterMouse"
python discover/discover.py --enumerate   # exits 0
python discover/discover.py --read-battery   # prints "Battery: N%"
```

2. Manual — review FINDINGS.md checklist:
- [ ] Usage Page field populated (e.g., 0xFF00)
- [ ] Battery Byte Offset is a specific number (not TBD)
- [ ] Value Range documented (0–100 or raw scale + formula)
- [ ] Confirmation method described
- [ ] C# translation block present
- [ ] FINDINGS.md is sufficient for a Phase 2 executor who has never run the discovery script
</verification>

<success_criteria>
- `python discover/discover.py --enumerate` exits 0 (device found)
- `python discover/discover.py --read-battery` prints a battery percentage matching the physical mouse's known charge level
- FINDINGS.md is complete: Usage Page, report ID, byte offset, value range, confirmation method, and C# translation all populated
- Phase 2 can begin: an executor reading only FINDINGS.md would know exactly which HID interface to open, which byte to read, and how to interpret the value
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-discovery/01-02-SUMMARY.md` using the summary template. This summary is the primary handoff document for Phase 2. Include:
- The confirmed byte offset (the single most important fact of this phase)
- The Usage Page and interface used
- The value range and any encoding notes
- The C# snippet verbatim from FINDINGS.md
- Any anomalies encountered (multiple vendor TLCs, unexpected byte encoding, etc.)
- Link to FINDINGS.md for full details
</output>

---
phase: 01-protocol-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - discover/discover.py
  - discover/requirements.txt
autonomous: true
requirements:
  - Prerequisite for HID-01
must_haves:
  truths:
    - "Running `python discover.py --enumerate` exits 0 and prints at least one TLC line for VID=0x3434 PID=0xD034"
    - "Running `python discover.py --enumerate` exits 1 with a clear error message when the dongle is not connected"
    - "The script skeleton contains the three discovery functions: enumerate_tlcs, scan_feature_reports, read_input_reports"
  artifacts:
    - path: "discover/discover.py"
      provides: "Discovery script with --enumerate flag and all three discovery functions"
      min_lines: 80
    - path: "discover/requirements.txt"
      provides: "Pinned hid package dependency"
      contains: "hid"
  key_links:
    - from: "discover/discover.py"
      to: "hid.enumerate()"
      via: "--enumerate CLI flag calling enumerate_tlcs()"
      pattern: "enumerate_tlcs"
    - from: "discover/discover.py"
      to: "sys.exit"
      via: "exit code 0 = device found, exit code 1 = not found"
      pattern: "sys\\.exit"
---

<objective>
Scaffold the Phase 1 discovery environment: a Python script with a working `--enumerate` flag that confirms the Keychron dongle is present and lists its TLCs, plus a requirements.txt that pins the `hid` package.

Purpose: Establish the automated verification baseline (Nyquist Wave 0). Every subsequent task in this phase runs `python discover.py --enumerate` to confirm the environment and device are intact before doing anything else.

Output: `discover/discover.py` (skeleton with all three discovery functions + `--enumerate` mode) and `discover/requirements.txt`.
</objective>

<execution_context>
@C:/Users/Bernhard/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Bernhard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-protocol-discovery/01-RESEARCH.md
@.planning/phases/01-protocol-discovery/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Check keychron-m3-linux main.py for known byte offset</name>
  <files>discover/FINDINGS.md</files>
  <action>
Before writing any code, fetch the reference implementation to extract the known battery byte offset.

Fetch: https://raw.githubusercontent.com/byte-bandit/keychron-m3-linux/main/main.py

If the fetch succeeds, scan for:
- A constant or variable named `battery` or containing `battery` — the byte index used to slice the input report
- Any `REPORT_ID` or similar constant
- The report reading loop and which byte index it uses for the percentage value

Create `discover/FINDINGS.md` using the template from RESEARCH.md. If the offset is found, fill in the "Battery Byte Offset" field and the "Source: keychron-m3-linux main.py (pre-confirmed)" note. If the fetch fails, leave the offset field as "TBD — to be determined empirically in Plan 02".

This file is the phase deliverable; creating the template now means Plan 02 only needs to fill in values, not create structure.
  </action>
  <verify>
    <automated>test -f "D:/OneDrive/Coding Projects/BatterMouse/discover/FINDINGS.md"</automated>
  </verify>
  <done>FINDINGS.md exists with the template populated; byte offset field is either a confirmed value from main.py or explicitly marked TBD.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write discover.py skeleton and requirements.txt</name>
  <files>discover/discover.py, discover/requirements.txt</files>
  <action>
Create `discover/requirements.txt`:
```
hid>=1.0.6
```

Create `discover/discover.py` using the complete skeleton from RESEARCH.md (the "Complete Discovery Script Skeleton" section). The skeleton already contains `enumerate_tlcs`, `read_input_reports`, and `scan_feature_reports` functions.

IMPORTANT additions to the skeleton:

1. Add a `--enumerate` CLI flag (use `argparse` or `sys.argv` check — keep it simple):
   - When `--enumerate` is passed, call `enumerate_tlcs()`, then filter for vendor TLCs.
   - If at least one vendor TLC is found: print "OK: {N} vendor TLC(s) found" and `sys.exit(0)`.
   - If no vendor TLCs found but device IS present: print "WARNING: device found but no vendor TLC — check usage_page filtering" and `sys.exit(1)`.
   - If device not found at all: print "ERROR: Keychron dongle not found (VID=0x3434 PID=0xD034)" and `sys.exit(1)`.

2. Preserve the `if __name__ == "__main__"` block that runs full discovery (enumerate → feature scan → 60s input report stream) when called WITHOUT `--enumerate`.

3. Add a module-level docstring explaining usage:
   ```
   Usage:
     python discover.py --enumerate    # Quick check: is device present?
     python discover.py                # Full discovery: enumerate + feature scan + 60s input stream
   ```

Anti-patterns from RESEARCH.md to avoid:
- Do NOT use `hid.device().open(VID, PID)` — always use `open_path()` with path from `enumerate()`
- Do NOT open Usage Page 0x0001 (Generic Desktop) — filter usage_page >= 0xFF00 only
- Do NOT interpret an empty read as an error — use `set_nonblocking(1)` and treat empty list as "mouse idle"

Install the dependency after writing the file:
```bash
pip install hid
```
  </action>
  <verify>
    <automated>cd "D:/OneDrive/Coding Projects/BatterMouse" && python discover/discover.py --enumerate</automated>
  </verify>
  <done>
- `discover/discover.py` exists with all three functions + `--enumerate` flag
- `discover/requirements.txt` exists with `hid>=1.0.6`
- `python discover/discover.py --enumerate` exits 0 and prints at least one vendor TLC line when dongle is connected, OR exits 1 with a clear error when dongle is absent
  </done>
</task>

</tasks>

<verification>
With the dongle connected and mouse on:
```bash
cd "D:/OneDrive/Coding Projects/BatterMouse"
python discover/discover.py --enumerate
```
Expected: exits 0, prints one or more lines containing "VENDOR (candidate)" and a path starting with `\\?\HID#`.

Structural check: `discover.py` must contain `open_path`, `usage_page >= 0xFF00`, `set_nonblocking`, `get_feature_report`, and `--enumerate` handling.
</verification>

<success_criteria>
- `python discover/discover.py --enumerate` returns exit code 0 with dongle connected
- Script contains all three discovery functions from RESEARCH.md skeleton
- `requirements.txt` pins the `hid` package
- FINDINGS.md template exists (byte offset populated if found in reference source, TBD otherwise)
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-discovery/01-01-SUMMARY.md` using the summary template. Include:
- Whether byte offset was found in keychron-m3-linux main.py (and the value if found)
- Actual vendor TLC usage_page(s) seen on this machine
- Any unexpected enumeration results
</output>
